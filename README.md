Для запуска проекта следует создать базу данных и указать название базы в файле `.env` (`.env` файл создать из копии .env.example), а затем выполнить следующие команды последовательно:

`php artisan migrate`
и
`php artisan db:seed`